import Layout from '@/components/Layout';

export default function SettingsPage() {
  return (
    <Layout>
      <div>
        <h1 className="text-2xl font-semibold text-gray-800 mb-4">Settings</h1>
        <p>User settings and application configuration options will appear here.</p>
        {/* Placeholder for API key management, preferences, etc. */}
      </div>
    </Layout>
  );
}
